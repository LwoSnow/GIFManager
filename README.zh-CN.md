# GIFManager

一个轻量的 Windows 表情包管理器，使用 **PySide6（Qt 6）** 构建。
界面仿照 QQ表情面板的样式：像电脑 QQ 一样直接浏览表情包、按分组管理，
并一键发送到任意聊天窗口。

**[English](README.md)**

---

## 功能特性

- **无边框暗/亮色界面** — 简洁的无边框窗口 + 可拖拽工具栏、Win11 圆角、
  暗色/亮色主题、现代化扁平风格。
- **底部分组标签栏** — 底部表情个性化分组。
  内置 **"全部"** 标签汇总所有分组的表情；**"默认表情"** 分组始终可用于快速导入。
- **两种分组类型**
  - *普通分组* — 存储 GIF/PNG/JPG/WebP/BMP 图片表情包。
  - *文字分组* — 存储纯文字（颜文字等）。在文字分组内按 **Ctrl+V** 粘贴，
    剪贴板的每一行都会批量保存为一条文字表情。
- **导入方式**
  - `导入GIF` 按钮 → *从文件夹导入*（递归扫描）或 *从文件导入*（支持多选）。
  - 直接把文件拖入窗口即可批量导入到当前分组（选中"全部"时导入到默认表情）。
  - 同分组内重复文件自动跳过（MD5 内容哈希）；同一文件可以存在于多个分组，
    相当于多标签。
- **发送到聊天** — 点击表情包即复制到剪贴板：
  - **文件路径模式**（默认，推荐）— QQ / 微信可直接粘贴。
  - **图片数据模式** — 复制图片像素数据，适用于需要图片数据的应用。
- **文字表情包** — 以紧凑的文字卡片展示而非图片缩略图，预览长度可配置
  （单行 / 多行上限）。
- **搜索** — 按文件名 / 文字内容实时过滤。
- **丰富的交互**
  - 右键表情包：发送、重命名/修改、移动到分组、删除。
  - 在"全部"视图中删除重复表情时，会弹出对话框勾选要删除的分组副本。
  - 拖拽表情可在列内排序；拖拽分组标签可调整分组顺序；
    右键分组可重命名/删除（内置分组受保护）。
  - **一键整理** — 按窗口宽度将表情均匀分配到各列。
  - 悬停 GIF 播放动画（按可见性驱动，屏幕外的 GIF 不会播放）。
- **系统托盘常驻** — 关闭窗口时隐藏到托盘；单击托盘图标（或按全局快捷键）
  切换显示/隐藏，托盘右键菜单可退出。
- **全局快捷键** — 默认 **F10** 呼出/隐藏窗口；支持组合键
  （如 `Ctrl+Shift+1`）、**鼠标侧键**（XButton1/XButton2），ESC 清空快捷键。
- **记忆系统** — 窗口大小/位置、上次选中的分组（可开关）、发送模式、主题、
  语言、快捷键都会在重启后恢复。
- **性能设计** — 懒加载（只创建可视区域内的卡片）、线程池缩略图解码
  （64 MB 像素缓存）、可配置 CPU 核心数、窗口缩放时自动融合列，布局不会错乱。
- **日志系统** — 日志按时间戳写入 `logs/` 目录，逐条 flush 崩溃安全，
  设置里可一键"清除所有日志"。

---

## 界面布局

```
+------------------------------------------------------------------+
| [ 🔍 搜索表情包...            ] [导入GIF▾] [+文字] [⚙]           |  ← 可拖拽工具栏
+------------------------------------------------------------------+
|                                                                  |
|   [GIF1]   [GIF2]   [GIF3]   [GIF4]    ← 图片表情（网格 / 列堆叠）|
|   [GIF5]   [GIF6]   ...                                          |
|   ┌──────────────────┐                                           |
|   │ (｡•̀ᴗ-)✧          │  ← 文字表情（紧凑卡片）                   |
|   └──────────────────┘                                           |
|                                                                  |
+------------------------------------------------------------------+
|  全部 | 默认表情 | 梗图 | 萌宠 | 颜文字 |  ＋                     |  ← 底部分组标签栏
+------------------------------------------------------------------+
|   默认表情 · 128 个表情包  |  共 256 个                           |  ← 状态栏
+------------------------------------------------------------------+
```

---

## 环境要求

- **Windows 10 / 11**（全局快捷键、托盘、圆角、开机自启依赖 Windows 特性；
  其余功能在其他平台也可运行）
- **Python 3.10+**（程序编写版本为Python 3.12.0）
- **PySide6 ≥ 6.5**

唯一的第三方依赖是 PySide6。

---

## 安装与运行

```bash
# 1. 创建虚拟环境（推荐）
python -m venv .venv

# 2. 激活（Windows）
.venv\Scripts\activate

# 3. 安装依赖
pip install PySide6

# 4. 运行
python main.py
```

如果项目已自带 `.venv`，可直接：

```bash
.venv\Scripts\python.exe main.py
```

首次启动会自动创建 `data/emoji.db`（SQLite，WAL 模式）和
`data/emojis/<分组名>/` 目录。

---

## 使用指南

### 导入表情包

| 操作 | 结果 |
| --- | --- |
| 点击 **导入GIF** → *从文件导入* | 多选 GIF/PNG/JPG/WebP/BMP，复制到当前分组 |
| 点击 **导入GIF** → *从文件夹导入* | 递归扫描文件夹内所有支持的图片 |
| 拖放文件到窗口 | 批量导入到当前分组（选中"全部"时导入到默认表情） |
| 选中**文字分组**后按 Ctrl+V | 剪贴板每行保存为一条文字表情 |

### 分组管理

- **全部** 为内置分组，不可删除/重命名；展示所有分组的表情，
  跨组重复的表情折叠为一张卡片。
- **默认表情** 是在"全部"状态下导入时的兜底目标分组。
- 点击标签栏右端 **＋** 可新建**普通分组**或**文字分组**。
- 拖拽标签调整顺序；右键标签可重命名/删除（内置分组受保护）。

### 发送表情包

1. 点击表情包 — 立即复制到剪贴板（状态栏会有提示）。
2. 在 **设置 → 发送** 中切换发送模式：*复制文件路径*（推荐）或 *复制图片数据*。
3. 到 QQ / 微信 / 任意聊天窗口粘贴。

> 提示：QQ 可以直接粘贴文件路径；微信同样可粘贴图片。
> 若某些应用不接受粘贴路径，请切换到"复制图片数据"模式。

### 快捷键

- **F10**（默认，可配置）：在任何地方呼出/隐藏窗口。
- 捕获快捷键时按 **ESC**：清空快捷键（即不设置）。
- 搜索框内右键显示本地化菜单（撤销/重做/剪切/复制/粘贴/删除/全选）。

---

## 数据存储与备份

```
GIFManager/
├── main.py                     # 程序入口
├── app/                        # 应用程序代码
│   ├── main_window.py          # 主窗口、托盘、热键、拖放
│   ├── models/
│   │   ├── data_manager.py     # SQLite + 文件管理、剪贴板
│   │   ├── lang_manager.py     # 多语言加载（language/*.json）
│   │   └── logger.py           # 崩溃安全日志
│   ├── widgets/                # 分组标签、表情网格、设置、热键等
│   └── theme/                  # 暗色 / 亮色 QSS
├── language/                   # 语言文件（zh_CN.json、en_US.json、...）
├── data/
│   ├── emoji.db                # SQLite 数据库（分组、表情、列信息）
│   └── emojis/<分组名>/        # 实际的表情文件，每个图片分组一个目录
├── logs/                       # 按时间戳的会话日志（自动创建）
└── icon.ico                    # 应用与托盘图标
```

**备份表情包时请整个复制 `data/` 目录** —— 它同时包含数据库和所有表情文件。
表情文件会被重命名为随机的 8 位十六进制名，分组归属记录在 `emoji.db` 中。

### SQLite 表结构（摘要）

- `groups(id, name, type, sort_order, is_builtin, created_at)` — `type` 为
  `'image'` 或 `'text'`。
- `emojis(id, group_id, filename, text_content, original_name, content_hash,
  sort_order, col_index, user_sorted, created_at)` — `content_hash` 用于同组查重
  以及"全部"视图的跨组折叠。

---

## 设置项（设置对话框 ⚙）

| 分类 | 设置项 | 说明                                                      |
| --- | --- |-----------------------------------------------------------|
| 通用 | 语言 | `zh_CN` / `en_US`（自动扫描 `language/*.json`），即时生效 |
| 通用 | 启动时恢复到上次选中的分组 | 默认开启                                                  |
| 通用 | 开机自启动 | 写入 Windows 注册表 Run 键                                |
| 通用 | 窗口置顶（始终在最前） | —                                                         |
| 通用 | 主题 | 暗色（默认）/ 亮色                                        |
| 通用 | 清除所有日志 | 删除 `logs/*.log`                                         |
| 发送 | 发送模式 | 复制文件路径（推荐）/ 复制图片数据                        |
| 快捷键 | 全局快捷键 | 默认 F10；支持组合键与鼠标侧键；ESC 清空                  |
| 文字 | 预览限制 | 文字表情卡片的单行 / 多行字符上限                         |
| 性能 | 多线程核心数 | 缩略图与批量导入使用的 CPU 核心数（0 = 自动）             |
| 关于 | — | 版本、协议                                                |

---

## 语言

翻译文件位于 `language/<code>.json`。程序会扫描该目录，并把所有可用语言列在
**设置 → 语言** 中。新增语言只需按 `zh_CN.json` 的键结构再创建一个 JSON 文件即可。

---

## 性能设计

- **懒加载渲染** — 只把可视区域（±300 px 缓冲）内的表情物化为控件；
  "全部"视图一次性创建全部卡片，但依赖像素缓存保持流畅。
- **线程池缩略图** — 解码/缩放运行在全局 `QThreadPool`（默认
  min(核心数−1, 8)），结果缓存在 64 MB 的 `QPixmapCache` 中。
- **按可见性播放 GIF** — 只播放视口内（±150 px）的 GIF，并发上限为核心数 × 2，
  其余显示首帧。
- **列融合** — 窗口变小时，无法完整显示的列自动并入前面的列（150 ms 防抖），
  卡片不会重叠。
- **批量导入** — 哈希与文件复制并行化；数据库插入在单个事务内完成并查重。

---

## 常见问题

- **快捷键注册失败** — 该组合键已被其他程序占用，请在设置里换一个快捷键。
- **粘贴"文件路径"在某些应用无效** — 请在 设置 → 发送 中切换到"复制图片数据"。
- **导入时似乎跳过了文件** — 与同分组内已有文件内容相同的文件会被当作重复项
  有意跳过。
- **GIF 不动画** — 为性能考虑只播放可视区内的 GIF；滚动到视野内就会开始播放。

---

## 开源许可

[MIT](LICENSE) © 2026 LwoSnow
